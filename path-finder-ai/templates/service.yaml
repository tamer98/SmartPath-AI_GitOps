apiVersion: v1
kind: Service
metadata:
  name: application-service
spec:
  type: ClusterIP
  selector:
    app: path-finder-ai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000      

      



