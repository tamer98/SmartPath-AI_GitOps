{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.mcp.enabled .Values.mcp.auth.enabled }}
kind: ClusterRoleBinding
apiVersion: {{ include "common.capabilities.rbac.apiVersion" . }}
metadata:
  name: {{ printf "%s-mcp-auth" (include "common.names.fullname.namespace" .) | trunc 63 | trimSuffix "-" }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: mcp
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ printf "%s-mcp-auth" (include "common.names.fullname.namespace" .) | trunc 63 | trimSuffix "-" }}
subjects:
  # Grant access to the MCP service account itself
  - kind: ServiceAccount
    name: {{ include "mongodb.mcp.serviceAccountName" . }}
    namespace: {{ include "mongodb.namespace" . }}
  {{- range .Values.mcp.auth.allowedServiceAccounts }}
  - kind: ServiceAccount
    name: {{ .name }}
    namespace: {{ .namespace | default (include "mongodb.namespace" $) }}
  {{- end }}
{{- end }}
